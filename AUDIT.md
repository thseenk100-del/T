# التدقيق الأولي — تحسينك

## SCAN

المشروع موقع ثابت متعدد الصفحات مبني بـ **HTML وCSS وJavaScript خام**، وليس React أو Vue أو إطارًا آخر. الصفحة الرئيسية تعتمد على `index.html`، مع صفحات `products.html` و`product.html` و`services.html` و`contact.html`. ملف التنسيق المستخدم فعليًا هو `css/styles.css`، بينما توجد نسخة أخرى باسم `css/style.css` تحتاج إلى التحقق من كونها غير مستخدمة. لا توجد أداة build أو lint للموقع الثابت؛ مجلد `telegram-bot.kee` مشروع Node مستقل بخدمة تشغيل منفصلة.

## DIAGNOSE — المرئي

1. توجد تدرجات خضراء متعددة وظلال عامة كثيرة تجعل الواجهة أقرب إلى قالب جاهز من هوية تحريرية واضحة.
2. توجد قواعد CSS متكررة ومتعارضة في `css/styles.css`، خصوصًا للبطاقات والأزرار والعناوين والاستجابة للشاشات الصغيرة.
3. المسافات كبيرة وغير متسقة، وبعض الأقسام تعتمد على بطاقات بيضاء وظلال متطابقة.
4. شبكة المنتجات أفقية، لكن التحكم بالسهمين لا يوضح الاتجاه بصريًا، والبطاقة لا تحتوي على تسلسل بصري كافٍ للسعر/التفاصيل.
5. العناوين العامة تستخدم خطوطًا وأحجامًا متعارضة، مع اعتماد Cairo وحده دون نظام أوزان واضح.
6. قسم الأسئلة الشائعة يكرر منطق JavaScript مرتين، كما أن أيقونة الفتح مخفية من CSS.
7. بعض الصفحات تحتوي على رموز emoji داخل المحتوى بدل أيقونات دلالية متسقة.
8. الصفحة الرئيسية تحتوي على تنسيقات inline، ومسار صورة hero مختلف عن بعض الصفحات الأخرى.
9. شريط التنقل يفتقد بنية/حالة نشطة واضحة، وبعض الصفحات تحتوي على زر قائمة للهاتف بينما الصفحة الرئيسية لا تحتوي عليه.
10. زر السلة يحقن أسلوبه عبر JavaScript، ما يجعل النظام البصري غير قابل للصيانة بسهولة.

## DIAGNOSE — هندسي ووظيفي

1. `js/main.js` يستدعي `menuToggle.addEventListener` دون التحقق من وجود العنصر؛ هذا يسبب خطأ JavaScript في الصفحات التي لا تحتوي على زر القائمة.
2. منطق كشف الأقسام في `js/main.js` يخفي جميع الأقسام عند التحميل وقد يسبب ظهورًا متأخرًا أو بقاء محتوى مخفيًا عند تعطل JavaScript.
3. النماذج والروابط تحتاج اختبارًا بعد أي تعديل، خصوصًا روابط WhatsApp وTelegram ومسارات GitHub Pages.
4. الأسئلة الشائعة تستخدم مستمعي أحداث منفصلين ومتناقضين؛ يجب توحيدها مع الحفاظ على السلوك الحالي.
5. لا توجد صفحة 404 مخصصة واضحة، ولا توجد مؤشرات كاملة للتركيز ولوحة المفاتيح.
6. يوجد مشروع Node مستقل داخل `telegram-bot.kee`، لذلك لن يتم تغيير تبعياته أو بنيته ضمن إعادة تصميم الموقع.

## PLAN

سيتم إجراء تحسينات صغيرة قابلة للمراجعة على الطبقة المرئية أولًا: توحيد المتغيرات والألوان والطباعة، إضافة حالات focus/hover، تحسين الحاويات والاستجابة، ضبط البطاقات والأقسام، ثم إصلاح أخطاء JavaScript الآمنة المرتبطة مباشرة بالواجهة. لن يتم تغيير الإطار أو إعادة كتابة المشروع أو إضافة مكتبات.

## معيار التحقق

يجب تشغيل الموقع محليًا، اختبار الصفحات الرئيسية على سطح المكتب والهاتف عبر المتصفح، فحص الروابط والأزرار والسلة والأسئلة الشائعة، ثم تشغيل أي فحوص متاحة والتحقق من عدم وجود أخطاء JavaScript ظاهرة.
